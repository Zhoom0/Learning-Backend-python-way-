#Ejercicio 1 (sin respuesta).
#  Reescribe esta anotación usando la forma moderna (con |):
#  una función que recibe edad: Optional[int].

#def registrar(edad: int | None) -> None:

#def formatear(valor: float | int) -> str:
    #return f"Valor {valor}"


diccionario: list[dict[str, str]] =[
    {"tipo", "persona"},
    {"tipo", "personita"}
]